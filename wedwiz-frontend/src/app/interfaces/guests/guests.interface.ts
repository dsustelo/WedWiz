export interface IGuests {
  id: number;
  name: string;
  email: string;
  phone: string;
  plus_one: boolean;
  name_plus_one: string;
  older_than_8: boolean;
  group_id: number;
  invite_sent: boolean;
}
